/* eslint-disable indent */
const index = (state, action) => {
    switch (action) {
        case 'AN_ACTION':
            return state;
        default:
            return state;
    }
};

export default index;